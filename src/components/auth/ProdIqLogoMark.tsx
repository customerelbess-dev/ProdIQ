import { ProdIqBrandLogo } from "@/components/ProdIqBrandLogo";

/** Auth / marketing panels — image + wordmark (32px icon). */
export function ProdIqLogoMark(_props?: { size?: number }) {
  return <ProdIqBrandLogo variant="inline" />;
}
